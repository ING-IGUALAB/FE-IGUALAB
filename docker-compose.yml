name: test-jenkins-api-dev
services:
  fastapi-app:
    build: .
    container_name: test-jenkins-api-dev
    env_file:
      - .env.dev
    networks:
      - proxy_net
    restart: unless-stopped

networks:
  proxy_net:
    external: true
